```
$ ./neotun -h
A new encrypted tunnel powered by AgentNEO

Usage:
  neotun [flags]
  neotun [command]

Available Commands:
  help        Help about any command
  panic

Flags:
  -h, --help            help for neotun
  -l, --local string    local address exposed a socks5 service (default ":10801")
  -p, --port uint16     account port
  -r, --remote string   remote server address
  -s, --secret string   account secret

Use "neotun [command] --help" for more information about a command.
```
